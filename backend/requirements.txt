# Backend dependencies (spec/backend.md B2, spec/local-dev.md L1).
# Python 3.11 baseline. Every version is pinned so the image is reproducible.

# --- Web / validation ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.12

# --- Persistence ---
SQLAlchemy==2.0.35
alembic==1.13.3
psycopg[binary]==3.2.3

# --- Document reading / OCR / export (spec/backend.md B4, B10) ---
# Installed now so the backend image is built once; the extraction, OCR and
# export slices consume these. No provider SDK is present by design (B2).
pdfplumber==0.11.4
pypdfium2==4.30.0
pytesseract==0.3.13
Pillow==10.4.0
reportlab==4.2.2

# --- HTTP client ---
# Used by the live AI adapter (app/ai/live.py) and by Starlette's TestClient.
# Runtime dependency, not test-only.
httpx==0.27.2

# --- Test tooling (spec/local-dev.md L2 backend/tests) ---
pytest==8.3.3
